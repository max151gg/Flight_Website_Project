<UserControl x:Class="AdminApp.UserControls.FlightPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdminApp.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="950"
             Canvas.Left="150">
    <UserControl.Resources>
        

        <!-- Image-only button (no default chrome) + hover feedback -->
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#22000000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#33000000"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Title label style -->
        <Style x:Key="TitleLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
    </UserControl.Resources>

    <Canvas>

        <!-- One "row card" -->
        <Border Width="900"
            Height="90"
            Canvas.Left="23"
            Canvas.Top="21"
            Background="White"
            BorderBrush="#DDDDDD"
            BorderThickness="1"
            CornerRadius="10">

            <!-- Use Grid INSIDE the border for clean alignment -->
            <Grid Margin="14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left image -->
                <Border Width="56" Height="56" CornerRadius="10" Background="#F3F3F3"
                    VerticalAlignment="Center">
                    <!--<Image Source="/Images/bookplace.png"
                       Stretch="Uniform"
                       Margin="8"/>-->
                </Border>

                <!-- Title -->
                <TextBlock Grid.Column="1"
                       Style="{StaticResource TitleLabelStyle}"
                       Text="Flight"
                       Margin="12,0,12,0"/>

                <!-- Right buttons -->
                <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">

                    

                    <Button x:Name="btnEdit"
                        Style="{StaticResource IconButtonStyle}"
                        ToolTip="Edit"
                        Margin="0,0,8,0">
                        <!--<Image Source="/Images/editicon.png" Width="18" Height="18"/>-->
                    </Button>

                    <Button x:Name="btnDelete"
                        Style="{StaticResource IconButtonStyle}"
                        ToolTip="Delete">
                        <!--<Image Source="/Images/deleteicon.png" Width="18" Height="18"/>-->
                    </Button>

                </StackPanel>
            </Grid>

        </Border>

    </Canvas>

</UserControl>
